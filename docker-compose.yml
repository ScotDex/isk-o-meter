services:
  eve-bot:
    build: .
    restart: always
    deploy:
      resources:
        limits:
          memory: 128M  
          cpus: '0.10' 
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"  